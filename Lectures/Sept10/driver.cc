// Copyright 2024 maximusf
// testing functions written on 9/10
#include<iostream>
using std::cout;
using std::endl;
#include"sept10.h" // ignore cpplint message about file path

int main() {
	cout << endl;
	OutputAlphabet();


// int main(int n, char * argv []) { // n holds the number of things held in the command line, char * argv [] holds what the values are
// 	for ( int i = 0; i < n; ++i )
// 		cout << argv[i] << endl;
		// for ex: run in command line ./a.out hey 1 yo (all in the same line)


	// int start, f;
	// for( int i = 0; i < 44; ++i ) {
	// 	start = clock(); // gets current time from system clock
	// 	f = FibonacciR(i);
	// 	cout << "It took " << clock() - start << " milliseconds to compute " 
	// 	<< f << " recursively " << endl;



	// 	start = clock();
	// 	f = FibonacciI(i);
	// 	cout << "It took " << clock() - start << " milliseconds to compute " 
	// 	<< f << " iteratively " << endl;
	// }
	return 0;
}
// g++ *.cc
// ./a.out
